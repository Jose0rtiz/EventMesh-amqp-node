
'use strict';

const { Payload, FieldValue, FieldArray, FieldTable } = require('./serialize/types');
const { Client } = require('./client');

module.exports = {
    FieldValue : FieldValue,
    FieldArray : FieldArray,
    FieldTable : FieldTable,
    Payload : Payload,
    Client : Client,

    /**
     * @deprecated The 'Types' export group is deprecated; use require('...').Payload instead of require('...').Types.Payload
     */
    Types: {
        Payload : Payload,
    },
};

